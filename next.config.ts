import type { NextConfig } from "next";

// The sheet fetch happens server-side, so there's nothing to configure for
// CORS, images, or rewrites. Kept for future tweaks.
const nextConfig: NextConfig = {};

export default nextConfig;
